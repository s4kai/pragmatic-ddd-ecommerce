meta {
  name: Add Item to Cart
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/cart/items
  body: json
  auth: none
}

headers {
  X-User-Id: {{userId}}
}

body:json {
  {
    "productId": "{{productId}}",
    "sku": "SKU-001",
    "quantity": 1
  }
}
