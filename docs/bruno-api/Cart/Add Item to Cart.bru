meta {
  name: Add Item to Cart
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/cart/items
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "productId": "{{productId}}",
    "sku": "SKU-001",
    "quantity": 1
  }
}
