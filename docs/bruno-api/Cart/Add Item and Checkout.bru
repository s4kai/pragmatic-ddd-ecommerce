meta {
  name: Add Item and Checkout
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/cart/items/checkout
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "productId": "{{productId}}",
    "sku": "SKU-001",
    "quantity": 1
  }
}
